//optimized code
pragma solidity 0.8.30;

contract C {
    uint[] x = new uint[](100);
      function p11() public view {
         uint y = x.length;
         for (uint i = 0; i < y; i++){
         }
    }
}